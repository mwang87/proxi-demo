%table.table.table-striped.table-condensed.tablesorter
    %thead
        %tr
            %th
            %th Dataset Name
    - @datasets.each do |dataset|
        %tr
            %td
                %a.btn.btn-default.pull-right{:style=>"margin-right: 20px; margin-bottom: 20px", :href => "/peptide/#{@peptide}/dataset/#{dataset.name}"} View Variants
            %td{:style=>"vertical-align:middle"}= dataset.name